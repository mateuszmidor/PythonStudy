# keys
quit_key = ord('q')

# strings
preview_window_name = "Preview"
capture_failed = "Capture failed"
template_preview_window_name = "Reference frame"
match_result_window_name = "Match result"

# BGR colors
green = (0, 255, 0)
red = (0, 0, 255)
yellow = (0, 255, 255)

# video overlay rectangle params
rectangle_thickness = 3